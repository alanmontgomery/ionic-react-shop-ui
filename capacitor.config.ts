import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-react-store-ui',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
